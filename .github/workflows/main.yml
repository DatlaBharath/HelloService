name: Basic Pipeline

on:
  push:
    branches:
      - main
  schedule:
    - cron: 'H/5 * * * *'

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: List directory contents
        run: ls